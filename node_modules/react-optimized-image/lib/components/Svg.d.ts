import React, { ReactElement } from 'react';
import { ImgSrc } from './types';
export interface SvgProps extends React.SVGProps<SVGSVGElement> {
    src: ImgSrc;
}
declare const Svg: ({ src, ...props }: SvgProps) => ReactElement;
export default Svg;
